<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.musicbeat.web.mapper.LabelMapper">

  <!--设置二级缓存-->
  <cache eviction="LRU" type="org.mybatis.caches.redis.RedisCache"/>

  <resultMap id="BaseResultMap" type="com.musicbeat.web.model.Label">
    <id column="id" javaType="java.lang.Integer" jdbcType="INTEGER" />
    <result column="gid" javaType="java.lang.Integer" jdbcType="INTEGER" />
    <result column="name" javaType="java.lang.String" jdbcType="VARCHAR" />
  </resultMap>

  <resultMap id="MusicLabelMap" type="com.musicbeat.web.model.Label" extends="BaseResultMap">
    <collection property="musics" ofType="com.musicbeat.web.model.Music">
      <id property="id" column="m_id"/>
      <result property="name" column="m_name"/>
      <result property="lyrics" column="m_lyrics"/>
      <result property="link" column="m_link"/>
    </collection>
  </resultMap>

  <sql id="Base_Column_List">
    id, lid, mid, name
  </sql>

  <select id="selectById" useCache="true" parameterType="Integer" resultMap="MusicLabelMap">
    select l.id,l.name,l.gid,m.id as m_id,m.name as m_name,m.lyrics as m_lyrics,m.link as m_link
    from label l left join music_label ml on l.id = ml.lid
    left join music m on ml.mid = m.id
    where l.id = #{id,jdbcType=INTEGER}
  </select>

  <delete id="deleteByPrimaryKey" flushCache="true" parameterType="java.lang.Integer">
    delete from label
    where id = #{id,jdbcType=INTEGER}
  </delete>

  <insert id="insert" flushCache="true" parameterType="com.musicbeat.web.model.Label">
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into label (lid, mid, name
      )
    values (#{lid,jdbcType=INTEGER}, #{mid,jdbcType=INTEGER}, #{name,jdbcType=VARCHAR}
      )
  </insert>

  <insert id="insertSelective" flushCache="true" parameterType="com.musicbeat.web.model.Label">
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into label
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="lid != null">
        lid,
      </if>
      <if test="mid != null">
        mid,
      </if>
      <if test="name != null">
        name,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="lid != null">
        #{lid,jdbcType=INTEGER},
      </if>
      <if test="mid != null">
        #{mid,jdbcType=INTEGER},
      </if>
      <if test="name != null">
        #{name,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>

  <update id="updateByPrimaryKeySelective" flushCache="true" parameterType="com.musicbeat.web.model.Label">
    update label
    <set>
      <if test="lid != null">
        lid = #{lid,jdbcType=INTEGER},
      </if>
      <if test="mid != null">
        mid = #{mid,jdbcType=INTEGER},
      </if>
      <if test="name != null">
        name = #{name,jdbcType=VARCHAR},
      </if>
    </set>
    where id = #{id,jdbcType=INTEGER}
  </update>

  <update id="updateByPrimaryKey" flushCache="true" parameterType="com.musicbeat.web.model.Label">
    update label
    set lid = #{lid,jdbcType=INTEGER},
      mid = #{mid,jdbcType=INTEGER},
      name = #{name,jdbcType=VARCHAR}
    where id = #{id,jdbcType=INTEGER}
  </update>

  <select id="selectByName" parameterType = "map" resultType="Integer">
    select id from label
    <where>
      <if test="name != null and name != ''">
        <choose>
          <when test="like">
            and name like "%"#{name,jdbcType=VARCHAR}"%"
          </when>
          <otherwise>
            and name = #{name,jdbcType=VARCHAR}
          </otherwise>
        </choose>
      </if>
    </where>
  </select>

  <select id="selectLabelByName" parameterType = "map" resultMap="MusicLabelMap">
    select l.id,l.name,l.gid,m.id as m_id,m.name as m_name,m.lyrics as m_lyrics,m.link as m_link
    from label l left join music_label ml on l.id = ml.lid
    left join music m on ml.mid = m.id
    <where>
      <if test="name != null and name != ''">
        <choose>
          <when test="like">
            and l.name like "%"#{name,jdbcType=VARCHAR}"%"
          </when>
          <otherwise>
            and l.name = #{name,jdbcType=VARCHAR}
          </otherwise>
        </choose>
      </if>
    </where>
  </select>

  <select id="selectAll" useCache="true" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List"/>
    from label
  </select>

</mapper>